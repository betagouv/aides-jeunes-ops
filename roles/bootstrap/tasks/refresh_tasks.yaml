- name: Refresh tasks
  block:
    - debug:
        msg: trigger nginx-restart
      notify: restart nginx
      changed_when: true
    - name: Refresh applications
      block:
        - name: node refresh app
          include_tasks: node_refresh.yaml
          with_items: "{{ applications }}"
          vars:
            - force: true
        #- name: openfisca refresh app
      